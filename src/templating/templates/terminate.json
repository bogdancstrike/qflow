{
  "primitive": "TERMINATE",
  "name": "Terminate",
  "description": "Immediately ends the workflow with a specified status and output. Used for validation failures, early exit conditions, or graceful abort.",
  "schema": {
    "task_ref": {"type": "string", "required": true},
    "type": {"type": "string", "const": "TERMINATE", "required": true},
    "termination_status": {
      "type": "string",
      "enum": ["COMPLETED", "FAILED", "TERMINATED"],
      "default": "FAILED"
    },
    "termination_reason": {"type": "string", "description": "Human-readable reason"},
    "output": {"type": "object", "description": "Final output attached to the terminated flow"}
  },
  "examples": [
    {
      "description": "Abort on empty transcription (used inside a SWITCH)",
      "task_ref": "abort_empty",
      "type": "TERMINATE",
      "termination_status": "FAILED",
      "termination_reason": "STT produced empty transcription",
      "output": {"error": "empty_transcription", "step": "stt"}
    }
  ]
}
