{
  "primitive": "LOG",
  "name": "Log",
  "description": "Emits a structured log entry into the task step log. Used for debugging, audit trails, and observability within the flow.",
  "schema": {
    "task_ref": {"type": "string", "required": true},
    "type": {"type": "string", "const": "LOG", "required": true},
    "input_parameters": {
      "type": "object",
      "required": true,
      "description": "Key-value pairs to log. 'message' key is used as the primary log line. All values support interpolation."
    }
  },
  "examples": [
    {
      "task_ref": "log_stt_complete",
      "type": "LOG",
      "input_parameters": {
        "message": "STT completed",
        "language_detected": "${steps.stt.output.language}",
        "text_length": "${len(steps.stt.output.text)}"
      }
    }
  ]
}
