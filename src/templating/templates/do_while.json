{
  "primitive": "DO_WHILE",
  "name": "Loop",
  "description": "Executes a set of tasks repeatedly while a condition remains true. Runs at least once (do-while semantics). Has a configurable max_iterations safety limit.",
  "schema": {
    "task_ref": {"type": "string", "required": true},
    "type": {"type": "string", "const": "DO_WHILE", "required": true},
    "loop_condition": {
      "type": "string",
      "required": true,
      "description": "Condition expression — loop continues while this evaluates to true"
    },
    "loop_over": {
      "type": "array",
      "required": true,
      "description": "List of tasks to execute each iteration"
    },
    "max_iterations": {
      "type": "integer",
      "default": 100,
      "description": "Safety limit to prevent infinite loops"
    }
  },
  "examples": [
    {
      "description": "Paginated API fetch",
      "task_ref": "paginated_fetch",
      "type": "DO_WHILE",
      "loop_condition": "${steps.paginated_fetch.output.iteration < steps.fetch_page.output.total_pages}",
      "max_iterations": 50,
      "loop_over": [
        {"task_ref": "fetch_page", "type": "HTTP", "input_parameters": {"http_request": {"method": "GET", "url": "${env.API_URL}/results?page=${steps.paginated_fetch.output.iteration}"}}}
      ]
    }
  ]
}
